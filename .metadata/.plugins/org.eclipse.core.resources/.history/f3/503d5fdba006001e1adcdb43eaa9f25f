<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ page import="java.io.File" %>
<%
    int bbsID = Integer.parseInt(request.getParameter("bbsID"));
    String fileName = request.getParameter("fileName");

    // 파일 삭제 로직 구현
    boolean success = deleteFile(bbsID, fileName);
    if (success) {
        out.println("true"); // 파일 삭제 성공 시 "true"를 응답으로 보냄
    } else {
        out.println("false"); // 파일 삭제 실패 시 "false"를 응답으로 보냄
    }

    // 파일 삭제 메서드
    public boolean deleteFile(int bbsID, String fileName) {
        // 데이터베이스 연결 및 파일 삭제 로직 작성
        // ...
        return true; // 파일 삭제 성공 시 true 반환, 실패 시 false 반환
    }
%>